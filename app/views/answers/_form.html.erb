<div class="answer_form">
<%= form_for [@question, @answer], remote: true do |f| %>
  <%= f.text_area :text, cols: 50, rows: 5 %><br>
  <%= f.submit :class => "btn btn-default btn-xs" %>
<% end %>
</div>
